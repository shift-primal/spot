# Port the HTTP/Socket.IO server listens on
PORT=
# Origin allowed by CORS (the client's URL)
CLIENT_ORIGIN=
# Path of the SQLite file, relative to server/ (default data/spot.db)
DB_PATH=
# Set to 1 when behind a proxy that sets X-Forwarded-For (the Vite dev proxy or a reverse proxy)
TRUST_PROXY=
