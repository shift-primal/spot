{
	"name": "server",
	"private": true,
	"version": "0.0.0",
	"type": "module",
	"scripts": {
		"dev": "node --env-file-if-exists=.env --watch src/index.ts",
		"start": "node --env-file-if-exists=.env src/index.ts",
		"build": "tsc --noEmit"
	},
	"dependencies": {
		"express": "^5.2.1",
		"socket.io": "^4.8.3",
		"@spot/shared": "workspace:*"
	},
	"devDependencies": {
		"@types/express": "^5.0.6",
		"@types/node": "^24.13.3",
		"typescript": "~6.0.2"
	}
}
