import { Canvas } from "#/components/canvas";
import { Container } from "#/components/container";

export const App = () => {
	return (
		<main>
			<Container>
				<Canvas />
			</Container>
		</main>
	);
};
